# AI Model Thresholds - Điều chỉnh độ tin cậy

# 1. SIMILARITY_THRESHOLD (0.0 - 1.0)
# Ngưỡng chấp nhận kết quả
# - Cao (0.7-0.8): Ít kết quả, nhưng chính xác cao
# - Trung bình (0.5-0.7): Cân bằng
# - Thấp (0.3-0.5): Nhiều kết quả, có thể sai
SIMILARITY_THRESHOLD=0.6

# 2. IMAGE_WEIGHT (0.0 - 1.0)
# Trọng số ảnh trong matching
# - Cao (0.7-0.9): Tập trung vào ảnh
# - Trung bình (0.5-0.7): Cân bằng
# - Thấp (0.3-0.5): Tập trung vào text
IMAGE_WEIGHT=0.6

# 3. TEXT_WEIGHT (0.0 - 1.0)
# Trọng số text mô tả
# Lưu ý: IMAGE_WEIGHT + TEXT_WEIGHT nên = 1.0
TEXT_WEIGHT=0.4

# Ví dụ các cấu hình:

# Tập trung ảnh (cho lỗi dễ nhận dạng bằng mắt):
# SIMILARITY_THRESHOLD=0.65
# IMAGE_WEIGHT=0.8
# TEXT_WEIGHT=0.2

# Tập trung text (cho lỗi cần mô tả chi tiết):
# SIMILARITY_THRESHOLD=0.55
# IMAGE_WEIGHT=0.4
# TEXT_WEIGHT=0.6

# Chính xác cao (ít false positive):
# SIMILARITY_THRESHOLD=0.75
# IMAGE_WEIGHT=0.7
# TEXT_WEIGHT=0.3

# Nhạy (nhiều suggestions):
# SIMILARITY_THRESHOLD=0.45
# IMAGE_WEIGHT=0.6
# TEXT_WEIGHT=0.4
